################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# samples/compiler_plugins/example/BUILD.bazel                                 #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_cc_library(
  NAME
    defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/src>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/src>"
  PUBLIC
)

iree_cc_library(
  NAME
    registration
  SRCS
    "src/PluginRegistration.cpp"
  DEPS
    ::defs
    MLIRIR
    iree::compiler::PluginAPI
  PUBLIC
)

iree_compiler_register_plugin(
  PLUGIN_ID
    example
  TARGET
    ::registration
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# The same source again as a loadable module, so both link modes can be tested.
# The id must differ; see src/PluginRegistration.cpp.
if(IREE_COMPILER_DYNAMIC_PLUGINS)
  iree_cc_library(
    NAME
      registration_dyn
    SRCS
      "src/PluginRegistration.cpp"
    COPTS
      "-DIREE_EXAMPLE_PLUGIN_ID=example_dyn"
    DEPS
      ::defs
      MLIRIR
      iree::compiler::PluginAPI
    PUBLIC
  )

  iree_compiler_register_dynamic_plugin(
    PLUGIN_ID
      example_dyn
    TARGET
      ::registration_dyn
  )
endif()
