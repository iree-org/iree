---
layout: default
permalink: Dialects/SequenceDialect
title: "'sequence' Dialect"
parent: Dialect Definitions
---

<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# 'sequence' Dialect
{: .no_toc }

A dialect designed to model iterable sequences.

The sequence dialect is used to model sequences that support high-level
operations such as `map`, `sum`, and `reduce`.

This dialect is currently under construction. Don't use it just yet.

1. TOC
{:toc}

## Type constraint definition

### of
Any type of sequence.

## Operation definition

### `sequence.map` (::mlir::iree_compiler::IREE::Sequence::MapOp)

applies a map function to a sequence element-wise


Syntax:

```
operation ::= `sequence.map` $mapping_function `,` $input_sequence  `:` type($input_sequence) `->` type($output_sequence) attr-dict
```

Returns a sequence resulting from applying a mapping function elementwise.

#### Attributes:

| Attribute | MLIR Type | Description |
| :-------: | :-------: | ----------- |
`mapping_function` | ::mlir::FlatSymbolRefAttr | flat symbol reference attribute

#### Operands:

| Operand | Description |
| :-----: | ----------- |
`input_sequence` | of

#### Results:

| Result | Description |
| :----: | ----------- |
`output_sequence` | of
