################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/amdgpu/device/BUILD.bazel                       #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

file(GLOB _GLOB_X_C LIST_DIRECTORIES false RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} CONFIGURE_DEPENDS *.c)
file(GLOB _GLOB_SUPPORT/X_C LIST_DIRECTORIES false RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} CONFIGURE_DEPENDS support/*.c)
file(GLOB _GLOB_X_H LIST_DIRECTORIES false RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} CONFIGURE_DEPENDS *.h)
file(GLOB _GLOB_SUPPORT/X_H LIST_DIRECTORIES false RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} CONFIGURE_DEPENDS support/*.h)
iree_amdgpu_binary(
  NAME
    amdgcn-amd-amdhsa--gfx1100
  TARGET
    amdgcn-amd-amdhsa
  ARCH
    gfx1100
  INTERNAL_HDRS
    "${_GLOB_SUPPORT/X_H}"
    "${_GLOB_X_H}"
  SRCS
    "${_GLOB_SUPPORT/X_C}"
    "${_GLOB_X_C}"
  COPTS

  LINKOPTS

)

iree_c_embed_data(
  NAME
    binaries
  SRCS
    "amdgcn-amd-amdhsa--gfx1100.so"
  C_FILE_OUTPUT
    "binaries.c"
  H_FILE_OUTPUT
    "binaries.h"
  IDENTIFIER
    "iree_hal_amdgpu_device_binaries"
  FLATTEN
  PUBLIC
)

iree_cc_library(
  NAME
    headers
  HDRS
    "${_GLOB_SUPPORT/X_H}"
    "${_GLOB_X_H}"
  DEPS

  PUBLIC
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
