@LIT_SITE_CFG_IN_HEADER@

import sys

config.iree_obj_root = "@IREE_BINARY_DIR@"
config.lib_dir = "@CMAKE_LIBRARY_OUTPUT_DIRECTORY@"
config.lib_ext = "@CMAKE_SHARED_LIBRARY_SUFFIX@"
config.python_executable = "@Python3_EXECUTABLE@"

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Let the main config do the real work
lit_config.load_config(config, "@IREE_SOURCE_DIR@/compiler/lit.cfg.py")