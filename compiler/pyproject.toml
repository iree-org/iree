[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cmake",
    "ninja",
    # MLIR build depends.
    "numpy",
    "packaging",
    "pybind11>=2.10.1",
    "PyYAML",
]
build-backend = "setuptools.build_meta"
