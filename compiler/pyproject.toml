[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cmake",
    "ninja",
    # MLIR build depends.
    "numpy",
    "packaging",
    "sympy",
]
build-backend = "setuptools.build_meta"
