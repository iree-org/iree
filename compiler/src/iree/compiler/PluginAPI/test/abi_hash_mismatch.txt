// The version matches, so the hash check is reached.

// RUN: not iree-opt --iree-load-plugin=%iree_test_abi_hash_mismatch_plugin 2>&1 | FileCheck %s

// CHECK: [IREE Dynamic Plugin ERROR]: plugin{{.*}}was built against plugin API headers 0000000000000000
